loglevel: debug
percentexclude: 30
environment: local
server:
  host:
  port: 8080

db:
  type: sql
  sql:
    driver: pgx
    name: banner
    user: postgres
    password: postgres
    host: postgres
    port: 5432

rmq:
  host: bas-rabbitmq
  port: 5672
  user: guest
  pswd: guest
